// This module is retired. The CRM grid tab-switcher has been replaced by
// flat top-level entity routes: /leads, /contacts, /accounts, /pipeline.
// Kept as an empty module to avoid breaking any stale import paths during transition.
