apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
spec:
  template:
    spec:
      containers:
      - name: backend
        env:
        - name: ATLAS_INIT_TOKEN
          value: "REPLACE_WITH_CI_TOKEN"
