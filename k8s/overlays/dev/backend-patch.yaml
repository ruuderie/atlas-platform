apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
spec:
  template:
    spec:
      containers:
      - name: backend
        envFrom:
        - configMapRef:
            name: app-config
        - secretRef:
            name: app-secrets
        # cloudflare-edge-secrets is not present in atlas-dev
        env:
        - name: ATLAS_INIT_TOKEN
          value: "REPLACE_WITH_CI_TOKEN"
