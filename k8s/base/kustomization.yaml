apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - backend.yaml
  - platform-admin.yaml
  - network-instance.yaml
  - anchor-app.yaml
  - folio.yaml
  - ingress-sidecar-rbac.yaml
  - ingress-sidecar.yaml
  # woodpecker-rbac.yaml intentionally excluded — applied once by admin, not managed by CI
